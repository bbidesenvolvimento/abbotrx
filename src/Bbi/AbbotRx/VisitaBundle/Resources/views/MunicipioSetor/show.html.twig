{% extends '::base.html.twig' %}

{% block body -%}
    <h1>MunicipioSetor</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Setoruf</th>
                <td>{{ entity.setorUf }}</td>
            </tr>
            <tr>
                <th>Principaisregioes</th>
                <td>{{ entity.principaisRegioes }}</td>
            </tr>
            <tr>
                <th>Uf</th>
                <td>{{ entity.uf }}</td>
            </tr>
            <tr>
                <th>Nomeconsultor</th>
                <td>{{ entity.nomeConsultor }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('municipiosetor') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('municipiosetor_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('municipiosetor_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}

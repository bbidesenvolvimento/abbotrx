{% extends '::base.html.twig' %}

{% block body -%}
    <h1>MunicipioSetor list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Setoruf</th>
                <th>Principaisregioes</th>
                <th>Uf</th>
                <th>Nomeconsultor</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('municipiosetor_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.setorUf }}</td>
                <td>{{ entity.principaisRegioes }}</td>
                <td>{{ entity.uf }}</td>
                <td>{{ entity.nomeConsultor }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('municipiosetor_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('municipiosetor_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('municipiosetor_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
